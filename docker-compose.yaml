version: '3.8'

services:
  web:
    build: .
    ports:
      - "5001:5001"
    volumes:
      # 手元のコード変更をコンテナ内に即時反映
      - .:/app
      # 生成された動画やDBを手元の data/ に永続化
      - ./data:/app/data
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=1
      - PYTHONUNBUFFERED=1